import React from 'react';

function LoadingProduct() {
    return (
        <React.Fragment>
            <p>Loading products...</p>
        </React.Fragment>
    );
}

export default LoadingProduct;
